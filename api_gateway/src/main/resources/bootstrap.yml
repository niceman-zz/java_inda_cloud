server:
  port: 8080

spring:
  application:
    name: api-gateway

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: true
  service-url:
    defaultzone: http://localhost:8761/eureka